# Session Handoff — 2026-07-19 (soir)

**Doc complète** : [`.context/HANDOFF-2026-07-19-football-bsd.md`](.context/HANDOFF-2026-07-19-football-bsd.md)

## État
- Football BSD fetcher **corrigé + pushé** (`378ea77`) — pas encore en prod VPS
- Build VPS **bloqué** : `pariscore-services` manquant (était dans `node_modules/` gitignored)
- Fix local prêt : `packages/pariscore-services/` + dep `workspace:*` dans `package.json`

## Reprise demain (ordre)
1. Commit/push le package workspace `pariscore-services`
2. VPS : `git pull && bun install && bun run build && pm2 restart pariscore-next`
3. Vérifier `/api/f1` + `/api/football/matches|live|prematch`
4. Ensuite : logos équipes foot

## Prompt
```
Lis .context/HANDOFF-2026-07-19-football-bsd.md et exécute Priorité 1 (deploy F1 shim + football BSD).
```
