# Scrapy configuration for PariScore — declarative project settings.
# Automatically created/standardized 2026-07-13.
# Run `scrapy` commands from this directory (scrapy_project/) or via the MCP
# server which sets cwd+SCRAPY_SETTINGS_MODULE for you.

[settings]
default = pariscore_scrapy.settings

[deploy]
# Scrapyd deployment target (unused locally; kept for future prod crawl server).
# url = http://localhost:6800/
project = pariscore_scrapy
